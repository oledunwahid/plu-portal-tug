-- CreateTable
CREATE TABLE "User" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "role" TEXT NOT NULL DEFAULT 'CASHIER',
    "outlet" TEXT NOT NULL,
    "active" BOOLEAN NOT NULL DEFAULT true,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);

-- CreateTable
CREATE TABLE "RequestBatch" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "title" TEXT NOT NULL,
    "requestType" TEXT NOT NULL,
    "status" TEXT NOT NULL DEFAULT 'PENDING',
    "outletGroup" TEXT NOT NULL,
    "cashierOutlet" TEXT NOT NULL,
    "userId" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "doneAt" DATETIME,
    "adminNote" TEXT,
    "exportedAt" DATETIME,
    "exportBatchId" TEXT,
    CONSTRAINT "RequestBatch_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE RESTRICT ON UPDATE CASCADE
);

-- CreateTable
CREATE TABLE "RequestBatchItem" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "batchId" TEXT NOT NULL,
    "code" TEXT,
    "codeIsAutoGenerated" BOOLEAN NOT NULL DEFAULT true,
    "name" TEXT NOT NULL,
    "category" TEXT NOT NULL,
    "department" TEXT NOT NULL,
    "price" REAL,
    "folder" TEXT,
    "serviceCharge" BOOLEAN NOT NULL DEFAULT true,
    "tax1" BOOLEAN NOT NULL DEFAULT true,
    "tax2" BOOLEAN NOT NULL DEFAULT true,
    "noDiscount" BOOLEAN NOT NULL DEFAULT true,
    "hideReceipt" BOOLEAN NOT NULL DEFAULT false,
    "printers" TEXT NOT NULL,
    "outlets" TEXT NOT NULL,
    "salesDef" TEXT NOT NULL DEFAULT 'SALES',
    "barcode" TEXT,
    "sortOrder" INTEGER NOT NULL DEFAULT 0,
    CONSTRAINT "RequestBatchItem_batchId_fkey" FOREIGN KEY ("batchId") REFERENCES "RequestBatch" ("id") ON DELETE CASCADE ON UPDATE CASCADE
);

-- CreateTable
CREATE TABLE "MasterItem" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "active" INTEGER NOT NULL DEFAULT 1,
    "code" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "category" TEXT NOT NULL,
    "department" TEXT NOT NULL,
    "salesDef" TEXT NOT NULL DEFAULT 'SALES',
    "price" REAL,
    "plu" TEXT,
    "barcode" TEXT,
    "uom" TEXT,
    "folder" TEXT,
    "serviceCharge" INTEGER NOT NULL DEFAULT 1,
    "tax1" INTEGER NOT NULL DEFAULT 1,
    "tax2" INTEGER NOT NULL DEFAULT 1,
    "noDiscount" INTEGER NOT NULL DEFAULT 1,
    "hideReceipt" INTEGER NOT NULL DEFAULT 0,
    "printers" TEXT,
    "outlets" TEXT,
    "outletGroup" TEXT,
    "importedAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL
);

-- CreateTable
CREATE TABLE "WikiArticle" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "title" TEXT NOT NULL,
    "content" TEXT NOT NULL,
    "category" TEXT NOT NULL,
    "isPinned" BOOLEAN NOT NULL DEFAULT false,
    "createdBy" TEXT NOT NULL,
    "updatedBy" TEXT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL
);

-- CreateTable
CREATE TABLE "GlossaryEntry" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "type" TEXT NOT NULL,
    "term" TEXT NOT NULL,
    "definition" TEXT NOT NULL,
    "defaultDefinition" TEXT,
    "grp" TEXT,
    "isSeeded" BOOLEAN NOT NULL DEFAULT false,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL
);

-- CreateTable
CREATE TABLE "PLURequest" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "requestType" TEXT NOT NULL,
    "status" TEXT NOT NULL DEFAULT 'PENDING',
    "code" TEXT,
    "codeIsAutoGenerated" BOOLEAN NOT NULL DEFAULT true,
    "name" TEXT NOT NULL,
    "category" TEXT NOT NULL,
    "department" TEXT NOT NULL,
    "price" REAL,
    "folder" TEXT,
    "serviceCharge" BOOLEAN NOT NULL DEFAULT true,
    "tax1" BOOLEAN NOT NULL DEFAULT true,
    "tax2" BOOLEAN NOT NULL DEFAULT true,
    "noDiscount" BOOLEAN NOT NULL DEFAULT true,
    "hideReceipt" BOOLEAN NOT NULL DEFAULT false,
    "printers" TEXT NOT NULL,
    "outlets" TEXT NOT NULL,
    "salesDef" TEXT NOT NULL DEFAULT 'SALES',
    "barcode" TEXT,
    "remarks" TEXT,
    "userId" TEXT NOT NULL,
    "outletGroup" TEXT NOT NULL,
    "cashierOutlet" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "doneAt" DATETIME,
    "adminNote" TEXT,
    "exportedAt" DATETIME,
    "exportBatchId" TEXT,
    CONSTRAINT "PLURequest_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE RESTRICT ON UPDATE CASCADE
);

-- CreateIndex
CREATE UNIQUE INDEX "User_email_key" ON "User"("email");

-- CreateIndex
CREATE UNIQUE INDEX "MasterItem_code_key" ON "MasterItem"("code");
